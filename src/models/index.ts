export interface HitokotoModel {
  commitFrom: string;
  createdAt: string;
  creator: string;
  creatorUid: number;
  from: string;
  fromWho: string;
  hitokoto: string;
  id: number;
  length: number;
  reviewer: number;
  type: string;
  uuid: string;
}
